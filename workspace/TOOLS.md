# 《工具安全原子红线》

## 1. 物理操作
- 闭环：读-写-验。严禁凭空假设文件内容。
- 路径：必须使用绝对物理路径。

## 2. 系统执行 (exec)
- 禁止：删除、格式化等破坏性指令。
- 隔离：严禁越界访问深层系统文件。

## 3. 技能规范
- 加载：通过 `read_file` 调阅 `SKILL.md`。
- 任务：时间约定必用 `cron`；后续处理必用 `defer`。
